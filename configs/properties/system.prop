# About phone definitions
ro.product.marketname=Redmi Note 13 4G
ro.infinity.soc=Snapdragon 685
ro.infinity.display=1080 x 2400, AMOLED 120Hz
ro.infinity.camera=108MP, 8MP, 2MP, 16MP

# Brightness Curve
persist.sys.brightness.low.gamma=true

# Camera
 persist.sys.cam.skip_detach_image=true

# Charger
ro.charger.enable_suspend=1

# CNE
persist.vendor.cne.feature=1

# Disable MTE Async for system server
persist.arm64.memtag.system_server=off

# Disable vsync for cpu rendered apps
debug.cpurend.vsync=false

# Ftrace
persist.debug.ftrace_events_disable=Yes

# Fingerprint
sys.fp.miui.token=0

# Deep Sleep
pm.sleep_mode=1
vendor.post_boot.parsed=1s

# Fwk-Detect
ro.vendor.qti.va_aosp.support=1

# Hint Manager
debug.hwui.use_hint_manager=true
debug.hwui.target_cpu_time_percent=30

# HWUI
debug.hwui.skia_atrace_enabled=false

# IMS
persist.dbg.ims_volte_enable=1
persist.dbg.volte_avail_ovr=1
persist.dbg.vt_avail_ovr=1

# Media
media.settings.xml=/vendor/etc/media_profiles_khaje_v0.xml
media.aac_51_output_enabled=true
media.stagefright.enable-aac=true
media.stagefright.enable-fma2dp=true
media.stagefright.enable-http=true
media.stagefright.enable-player=true
media.stagefright.enable-qcp=true
media.stagefright.enable-scan=true
media.stagefright.thumbnail.prefer_hw_codecs=true

# NTP
persist.backup.ntpServer="0.pool.ntp.org"

# Pre-Rendering
ro.vendor.perf.scroll_opt=true
persist.sys.perf.scroll_opt.heavy_app=1
persist.sys.default_scaling_gov=true

# SdCardFS
persist.sys.fuse.passthrough.enable=true

# Soc
ro.soc.manufacturer=QTI

# Sensors
persist.vendor.sensors.enable.mag_filter=true

# SSR
persist.vendor.ssr.enable_ramdumps=1
persist.vendor.ssr.restart_level=ALL_ENABLE

# WLAN
ro.hardware.wlan.dbs=0
ro.hardware.wlan.mimo=0
ro.hardware.wlan.vendor=qcom
